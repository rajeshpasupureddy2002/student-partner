<div class="dashboard-container">

    <!-- Reuse Sidebar (Mock Include or Partial if split) -->
    <!-- ideally we use a partial, but for now assuming layout handles it or we duplicate structure if needed. 
       Actually, dashboard layout handles the wrapper? No, dashboard layout usually just {{body}}.
       Wait, previous dashboard.hbs had the sidebar inside it. 
       To avoid duplication, I should really extract sidebar to a partial. 
       But to keep it simple and working NOW, I will use a simplified container and assume the layout handles global styles.
  -->

    <!-- SIDEBAR (Simplified for Settings Page or Layout should handle navigation) -->
    <!-- NOTE: Ideally the sidebar should be in the layout or a partial. 
       For this Step, I'll add a 'Back to Dashboard' floating button if Sidebar isn't global.
       Checking layout: layout just outputs body. dashboard.hbs had sidebar. 
       I will duplicate Sidebar for now to ensure consistency, but later refactoring to partials is better.
  -->

    <aside class="sidebar">
        <div class="sidebar-header">
            <div class="logo">
                <i class="fa-solid fa-graduation-cap"></i>
                <span>StudentPartner</span>
            </div>
        </div>

        <div class="profile-section">
            <div class="avatar">
                <img src="https://ui-avatars.com/api/?name={{user.name}}&background=random" alt="Profile">
            </div>
            <div class="user-info">
                <h3>{{user.name}}</h3>
                <p>{{user.email}}</p>
            </div>
        </div>

        <nav class="sidebar-menu">
            <a href="/dashboard" class="menu-item">
                <i class="fa-solid fa-house"></i>
                <span>Overview</span>
            </a>
            <a href="/dashboard/settings" class="menu-item active">
                <i class="fa-solid fa-gear"></i>
                <span>Settings</span>
            </a>
        </nav>

        <div class="sidebar-footer">
            <a href="/auth/logout" class="menu-item logout">
                <i class="fa-solid fa-arrow-right-from-bracket"></i>
                <span>Logout</span>
            </a>
        </div>
    </aside>

    <!-- LIST SETTINGS CONTENT -->
    <main class="main-content">
        <header class="top-header">
            <div class="welcome-text">
                <h1>Account Settings</h1>
                <p>Manage your profile and preferences.</p>
            </div>
        </header>

        {{#if success}}
        <div class="alert success"><i class="fa-solid fa-check-circle"></i> {{success}}</div>
        {{/if}}
        {{#if error}}
        <div class="alert error"><i class="fa-solid fa-triangle-exclamation"></i> {{error}}</div>
        {{/if}}

        <div class="settings-container glass-card">

            <!-- TABS HEADERS -->
            <div class="settings-tabs">
                <button class="tab-btn active" onclick="openTab(event, 'profile')">
                    <i class="fa-regular fa-id-card"></i> Profile
                </button>
                <button class="tab-btn" onclick="openTab(event, 'security')">
                    <i class="fa-solid fa-shield-halved"></i> Security
                </button>
                <button class="tab-btn" onclick="openTab(event, 'notifications')">
                    <i class="fa-regular fa-bell"></i> Notifications
                </button>
            </div>

            <form action="/dashboard/settings" method="POST" class="settings-form">

                <!-- PROFILE TAB -->
                <div id="profile" class="tab-content active">
                    <div class="form-row">
                        <div class="form-group">
                            <label>Full Name</label>
                            <div class="input-wrapper">
                                <i class="fa-regular fa-user"></i>
                                <input type="text" name="name" value="{{user.name}}" required>
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Phone Number</label>
                            <div class="input-wrapper">
                                <i class="fa-solid fa-phone"></i>
                                <input type="tel" name="phone" value="{{user.phone}}" placeholder="+1 234 567 890">
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label>Bio</label>
                        <div class="input-wrapper">
                            <i class="fa-regular fa-comment-dots"></i>
                            <textarea name="bio" rows="3"
                                placeholder="Tell us about yourself...">{{user.bio}}</textarea>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label>College / University</label>
                            <div class="input-wrapper">
                                <i class="fa-solid fa-building-columns"></i>
                                <input type="text" name="college" value="{{user.college}}"
                                    placeholder="Example University">
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Major / Stream</label>
                            <div class="input-wrapper">
                                <i class="fa-solid fa-book"></i>
                                <input type="text" name="major" value="{{user.major}}" placeholder="Computer Science">
                            </div>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label>LinkedIn URL</label>
                            <div class="input-wrapper">
                                <i class="fa-brands fa-linkedin"></i>
                                <input type="url" name="linkedin" value="{{user.linkedin}}"
                                    placeholder="https://linkedin.com/in/...">
                            </div>
                        </div>
                        <div class="form-group">
                            <label>GitHub URL</label>
                            <div class="input-wrapper">
                                <i class="fa-brands fa-github"></i>
                                <input type="url" name="github" value="{{user.github}}"
                                    placeholder="https://github.com/...">
                            </div>
                        </div>
                    </div>
                </div>

                <!-- SECURITY TAB -->
                <div id="security" class="tab-content">
                    <div class="info-box">
                        <i class="fa-solid fa-lock"></i>
                        <p>To change your password, please use the "Forgot Password" flow on the login page for now.</p>
                    </div>
                    <div class="form-group disabled">
                        <label>Email Address (Cannot be changed)</label>
                        <div class="input-wrapper">
                            <i class="fa-regular fa-envelope"></i>
                            <input type="email" value="{{user.email}}" disabled>
                        </div>
                    </div>
                </div>

                <!-- NOTIFICATIONS TAB -->
                <div id="notifications" class="tab-content">
                    <div class="toggle-group">
                        <div class="toggle-info">
                            <h4>Email Notifications</h4>
                            <p>Receive updates about course deadlines and announcements.</p>
                        </div>
                        <label class="switch">
                            <input type="checkbox" name="notifications_email" {{#if
                                user.notifications_email}}checked{{/if}}>
                            <span class="slider round"></span>
                        </label>
                    </div>

                    <div class="toggle-group">
                        <div class="toggle-info">
                            <h4>Push Notifications</h4>
                            <p>Get real-time alerts on your dashboard.</p>
                        </div>
                        <label class="switch">
                            <input type="checkbox" name="notifications_push" {{#if
                                user.notifications_push}}checked{{/if}}>
                            <span class="slider round"></span>
                        </label>
                    </div>
                </div>

                <!-- ACTION BUTTONS -->
                <div class="form-actions">
                    <button type="submit" class="btn-save">
                        <i class="fa-solid fa-floppy-disk"></i> Save Changes
                    </button>
                </div>

            </form>
        </div>
    </main>
</div>

<script>
    function openTab(evt, tabName) {
        const tabContents = document.querySelectorAll('.tab-content');
        const tabBtns = document.querySelectorAll('.tab-btn');

        tabContents.forEach(content => content.classList.remove('active'));
        tabBtns.forEach(btn => btn.classList.remove('active'));

        document.getElementById(tabName).classList.add('active');
        evt.currentTarget.classList.add('active');
    }
</script>